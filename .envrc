# Load the Vector dev shell from flake.nix (Rust 1.92, native deps, cross, cargo-nextest, …).
# Requires: direnv (https://direnv.net/) and nix with flakes enabled.
#
# Minimal build-only shell: use flake .#build

use flake
